# operation-fetch-model-objects

Lifecycle: single

Takes a guid list of object as input, checks if guid is native or external, and             instantiates and optionally selects objects before returning them.
