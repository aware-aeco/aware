# bim-model-data-product-client-create-model-async

Lifecycle: single

Creates a new model in the Bim Model Data Product and uploads a file as its first version.             Model creation will remain in a pending state until the file upload is completed successfully.
