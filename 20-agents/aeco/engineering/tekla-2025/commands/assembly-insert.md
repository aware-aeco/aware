# assembly-insert

Lifecycle: single

Inserts the  instance in the model.             At the moment only rebar assemblies can be inserted.             The main object of the assembly must be a  or .             If the main rebar is not set (null), the main rebar will be automatically set to the secondary rebar with the biggest weight.
