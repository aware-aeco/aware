---
name: allplan-utils-dockingpointutil
description: This skill encodes the allplan 2024.0 surface of the Utils.DockingPointUtil namespace — 1 types with full vendor-documented methods, properties, events, and behavioral remarks. Read when composing apps that touch any of: DockingPointUtil.
---

# Utils.DockingPointUtil

Auto-generated from vendor docs for allplan 2024.0. 1 types in this namespace.

## DockingPointUtil (class)

Implementation of the docking point utilities

[Vendor docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/)

### Methods
#### `__get_arc2d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Arc2D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_arc2d_docking_points)

#### `__get_arc3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Arc3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_arc3d_docking_points)

#### `__get_brep3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (BRep3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_brep3d_docking_points)

#### `__get_bspline2d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (BSpline2D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_bspline2d_docking_points)

#### `__get_bspline3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (BSpline3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_bspline3d_docking_points)

#### `__get_line2d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Line2D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_line2d_docking_points)

#### `__get_line3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Line3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_line3d_docking_points)

#### `__get_poly_point_curve2d_docking_points(element_key, points)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (object) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_poly_point_curve2d_docking_points)

#### `__get_poly_point_curve3d_docking_points(element_key, points)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (object) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_poly_point_curve3d_docking_points)

#### `__get_polygon2d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Polygon2D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_polygon2d_docking_points)

#### `__get_polygon3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Polygon3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_polygon3d_docking_points)

#### `__get_polyhedron3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Polyhedron3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_polyhedron3d_docking_points)

#### `__get_polyline2d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Polyline2D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_polyline2d_docking_points)

#### `__get_polyline3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Polyline3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_polyline3d_docking_points)

#### `__get_spline2d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Spline2D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_spline2d_docking_points)

#### `__get_spline3d_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Spline3D) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.__get_spline3d_docking_points)

#### `get_docking_points(element_key, element)`

get the docking points

**Remarks:** get the docking points

**Parameters:**
- `element_key` (str) — geometry element key
- `element` (Any) — geometry element

**Returns:** `List[Tuple[str, Point3D]]` — list with the docking points

[Docs](https://pythonparts.allplan.com/2024/api_reference/Utils/DockingPointUtil/DockingPointUtil/#Utils.DockingPointUtil.DockingPointUtil.get_docking_points)

