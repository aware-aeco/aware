---
name: allplan-handledirection
description: This skill encodes the allplan 2024.0 surface of the HandleDirection namespace — 1 types with full vendor-documented methods, properties, events, and behavioral remarks. Read when composing apps that touch any of: HandleDirection.
---

# HandleDirection

Auto-generated from vendor docs for allplan 2024.0. 1 types in this namespace.

## HandleDirection (enum)

Class with the definition of the handle directions.

[Vendor docs](https://pythonparts.allplan.com/2024/api_reference/GeneralScripts/HandleDirection/HandleDirection/)

### Values
- `ANGLE` = `angle` — Move the handle in xy direction. Show a circle as info.
- `CLICK` = `13` — handle can be used as button, ...
- `PLANE_DIR` = `plane_dir` — Move the handle in xy direction on the defined plane from HandleProperties Draw an arrow symbol in reference-handle point-direction
- `POINT_DIR` = `point_dir` — Move the handle in point direction, defined by the reference and handle point. Draw an arrow symbol in reference-handle point-direction
- `VECTOR_DIR` = `vector_dir` — Move the handle in the direction defined by the vector from HandleProperties
- `XYZ_DIR` = `xyz_dir` — Move the handle in xyz direction.
- `XY_DIR` = `xy_dir` — Move the handle in xy direction. Draw an arrow symbol in reference-handle point-direction
- `XZ_DIR` = `xz_dir` — Move the handle in xz direction. Draw an arrow symbol in reference-handle point-direction
- `X_DIR` = `x_dir` — Move the handle in x-direction. Draw an arrow symbol in x-direction
- `YZ_DIR` = `yz_dir` — Move the handle in yz direction. Draw an arrow symbol in reference-handle point-direction
- `Y_DIR` = `y_dir` — Move the handle in y-direction. Draw an arrow symbol in y-direction
- `Z_COORD` = `z_coord` — Move the handle in point direction, defined by the reference and handle point. Draw an arrow symbol in reference-handle point-direction
- `Z_DIR` = `z_dir` — Move the handle in z-direction. Draw an arrow symbol in z-direction
- `angle` = `9` — deprecated, use XYZ_DIR
- `plane_dir` = `11` — deprecated, use PLANE_DIR
- `point_dir` = `8` — deprecated, use POINT_DIR
- `vector_dir` = `12` — deprecated, use VECTOR_DIR
- `x_dir` = `1` — deprecated, use X_DIR
- `xy_dir` = `4` — deprecated, use XY_DIR
- `xyz_dir` = `7` — deprecated, use XYZ_DIR
- `xz_dir` = `5` — deprecated, use XZ_DIR
- `y_dir` = `2` — deprecated, use Y_DIR
- `yz_dir` = `6` — deprecated, use YZ_DIR
- `z_coord` = `10` — deprecated, use XYZ_DIR
- `z_dir` = `3` — deprecated, use Z_DIR

