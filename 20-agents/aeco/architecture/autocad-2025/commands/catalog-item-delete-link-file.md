# catalog-item-delete-link-file

Lifecycle: single

This function permanently deletes the link file if this item is a linked item.  If recursive is true, this function also deletes the link files referenced by this item's children. Otherwise, it deletes only the link file of this item.
