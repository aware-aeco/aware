# tool-create-acad-tool

Lifecycle: single

Creates the COM object that implements IAcadTool.If loadData is true, loads the tool’s data.  If the function succeeds, the return value is nonzero. If the function fails, the return value is zero.
