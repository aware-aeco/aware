# ordered-set1-symmetric-except-with

Lifecycle: single

Modifies the current set so that it contains only elements that are present either in the current set or in the specified collection, but not both.
