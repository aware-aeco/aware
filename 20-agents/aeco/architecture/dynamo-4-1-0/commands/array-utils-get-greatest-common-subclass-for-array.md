# array-utils-get-greatest-common-subclass-for-array

Lifecycle: single

If an empty array is passed, the result will be null             if there are instances, but they share no common supertype the result will be var
