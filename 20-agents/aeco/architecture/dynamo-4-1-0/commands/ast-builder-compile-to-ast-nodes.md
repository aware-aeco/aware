# ast-builder-compile-to-ast-nodes

Lifecycle: single

Compile a collection of NodeModel to AST nodes in different contexts.             If the context is ForNodeToCode, nodes should already be sorted in              topological order.
