# core-utils-create-node-from-string

Lifecycle: single

Given a name or string of names, this creates an IdentifierNode or IdentifierListNode             e.g. Creates an IdentifierNode from A and IdentifierListNode from A.B
