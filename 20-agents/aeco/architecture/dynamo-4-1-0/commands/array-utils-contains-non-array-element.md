# array-utils-contains-non-array-element

Lifecycle: single

If the passed in value is not an array or an empty array or an array which contains only empty arrays, return false.             Otherwise, return true;
