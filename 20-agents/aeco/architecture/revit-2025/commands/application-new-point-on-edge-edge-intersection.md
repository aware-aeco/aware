# application-new-point-on-edge-edge-intersection

Lifecycle: single

Construct a PointOnEdgeEdgeIntersection object which is used to define the placement of a ReferencePoint given two references to edge.
