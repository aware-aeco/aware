# gh-abstract-graph-prepare-for-use

Lifecycle: single

This function is called when the graph is about to be used for the first time,   maybe more often. You're supposed to reset all defaults. By default, this function   calls ClearGrips(), CreateGrips() and UpdateEquation()
