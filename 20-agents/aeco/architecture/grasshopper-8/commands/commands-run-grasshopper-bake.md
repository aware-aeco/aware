# commands-run-grasshopper-bake

Lifecycle: single

Run the _GrasshopperBake command logic.   This command requires either the BakeDocument or the BakeObject fields to be set.   This command is typically run from within the Grasshopper GUI, it should be hidden from the user.
