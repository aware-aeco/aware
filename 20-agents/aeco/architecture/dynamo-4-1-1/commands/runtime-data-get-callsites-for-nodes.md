# runtime-data-get-callsites-for-nodes

Lifecycle: single

This API is used by host integrations such as for Revit and C3D.             It is used to get the trace data list for all nodes binding to elements in the host.
