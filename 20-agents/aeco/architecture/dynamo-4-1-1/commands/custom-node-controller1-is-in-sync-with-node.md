# custom-node-controller1-is-in-sync-with-node

Lifecycle: single

Return if the custom node instance is in sync with its definition.               It may be out of sync if .dyf file is opened and updated and then               .dyn file is opened.
