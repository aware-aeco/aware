# ilibrary-view-customization-register-resource-stream

Lifecycle: single

Registers a given resource stream for a given url. If registered              successfully the requested resource is returned from the given stream.
