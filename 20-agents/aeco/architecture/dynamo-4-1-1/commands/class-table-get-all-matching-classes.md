# class-table-get-all-matching-classes

Lifecycle: single

Returns all matching classes for the given name from this ClassTable.             If the classes have a common base class, this simply returns the given name of the class.
