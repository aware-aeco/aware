# build-status-log-symbol-conflict-warning

Lifecycle: single

Logs the warning where the usage of a symbol (symbolName) cannot be              resolved because it collides with multiple symbols(collidingSymbolNames)
