# dsarray-get-value-from-indices

Lifecycle: single

= array[index1][index2][...][indexN], and             indices = {index1, index2, ..., indexN}                          Note this function doesn't support the replication of array indexing.
