---
name: yard-sketchup-classification-schema
description: Sketchup::ClassificationSchema API reference (YARD)
---

# Sketchup::ClassificationSchema API reference

The ClassificationSchema class represent schemas loaded in the model.

## Methods

- `<=>` — The <=> method is used to compare two ClassificationSchema objects for sorting. The comparison is done based on the schema name.
- `name` — The name method returns the name of the schema.
- `namespace` — The namespace method returns the namespace of the schema.
