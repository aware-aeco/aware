---
name: yard-string
description: String API reference (YARD)
---

# String API reference

The String class contains a method used to parse a string as a length value. All string arguments in Ruby API expect utf-8 strings.

## Methods

- `to_l` — The to_l method is used to convert a string to a length. The returned length is expressed in the Model units.
