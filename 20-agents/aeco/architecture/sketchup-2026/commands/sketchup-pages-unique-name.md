# sketchup-pages-unique-name

Lifecycle: single

The #unique_name method is used to generate a unique name for the page based on a base_name string. For example, a base name of “Joe” might return “Joe 2” if “Joe” already exists.
