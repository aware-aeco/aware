# sketchup-overlay-get-extents

Lifecycle: single

The method should be implementing sub-classes ensure what is drawn in 3D space doesn't appear clipped. If the overlay only draws in 2D this isn't needed.
