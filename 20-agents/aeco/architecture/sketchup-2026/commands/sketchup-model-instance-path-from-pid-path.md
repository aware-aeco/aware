# sketchup-model-instance-path-from-pid-path

Lifecycle: single

The #instance_path_from_pid_path method returns a instance path given a string with persistent ids representing the path to the entity.
