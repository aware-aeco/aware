# sketchup-dimension-linear-offset-vector

Lifecycle: single

The offset_vector= method is used to set the parallel offset vector from the reference line to the dimension line measured from the 'start' reference point.
