# sketchup-component-definition-load-time

Lifecycle: single

The #load_time method gets the load time of the component definition. For an internal component definition, this is the time that it was created. For an external component definition, this is the time that it was added to the model.
