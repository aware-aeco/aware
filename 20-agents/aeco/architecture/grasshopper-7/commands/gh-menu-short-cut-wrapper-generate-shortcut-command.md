# gh-menu-short-cut-wrapper-generate-shortcut-command

Lifecycle: single

Call this function to test all menu items for short-cut matches.   If the shortcut key-combo in the keyCode enumeration can be matched to a menu item,   the PerformClick() method on that item will be called. Disabled and hidden items   will be skipped entirely.
