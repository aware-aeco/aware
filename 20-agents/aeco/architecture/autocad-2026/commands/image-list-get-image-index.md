# image-list-get-image-index

Lifecycle: single

If exactMatch is FALSE, and if the image with the same size is not found, then the index of the image with the next highest size is returned. If the next highest size is not available, then the index of the image with the next lowest size is returned.
