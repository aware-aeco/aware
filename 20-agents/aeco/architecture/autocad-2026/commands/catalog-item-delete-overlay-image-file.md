# catalog-item-delete-overlay-image-file

Lifecycle: single

Permanently deletes the overlay image file of this item and, optionally, those of its children.  If recursive is true, this function recursively deletes the overlay image files of this item's children. Otherwise, it deletes the overlay image file of this item only.
