# sketchup-definitions-observer-abstract-on-component-properties-changed

Lifecycle: single

The #onComponentPropertiesChanged method is called whenever a definition's name or description are changed.
