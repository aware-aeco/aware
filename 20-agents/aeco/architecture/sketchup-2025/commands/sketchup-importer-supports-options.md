# sketchup-importer-supports-options

Lifecycle: single

This method is called by SketchUp to determine if the “Options” button inside the File > Import dialog should be enabled while your importer is selected.
