# sketchup-model-observer-abstract-on-place-component

Lifecycle: single

The #onPlaceComponent method is invoked when a component is “placed” into the model, meaning it is dragged from the Component Browser.
