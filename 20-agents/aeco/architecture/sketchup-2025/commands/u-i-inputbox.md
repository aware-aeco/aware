# u-i-inputbox

Lifecycle: single

The method intelligently handles various types for default values and lists, automatically attempting to convert provided inputs to match the type of the default value. This ensures consistency in data types throughout the operation.
