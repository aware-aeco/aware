# layout-style-set-dimension-units

Lifecycle: single

The #set_dimension_units method sets the unit format and precision for dimensions. Units may be for either LinearDimensions or AngularDimensions, but not both.
