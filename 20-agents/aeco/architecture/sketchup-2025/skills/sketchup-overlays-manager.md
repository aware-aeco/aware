---
name: yard-sketchup-overlays-manager
description: Sketchup::OverlaysManager API reference (YARD)
---

# Sketchup::OverlaysManager API reference

An overlay added to a model is invalidated once it's removed from the model. It cannot be re-added or added to another model. Create another unique instance for that.

## Methods

- `[]` — 
- `add` — Returns false if an overlay with the same id already exists.
- `each` — 
- `remove` — 
- `size` — 
