---
name: xeokit-sdk-camera-path
description: CameraPath declarations from xeokit-sdk
---

# CameraPath

## Methods

- `saveFrame(t: number)`
- `addFrame(t: number, eye: number, look: number[], up: number[])`
- `addFrames(frames: Frame[])`
- `loadFrame(t: number)`
- `sampleFrame(t: number, eye: number[], look: number[], up: number[])`
- `smoothFrameTimes(duration: number)`
- `clearFrames()`
