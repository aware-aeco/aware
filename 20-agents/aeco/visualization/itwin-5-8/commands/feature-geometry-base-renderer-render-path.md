# feature-geometry-base-renderer-render-path

Lifecycle: single

FeatureGeometryBaseRenderer.renderPath(geometryLengths: number[], geometryCoords: number[], fill: boolean, stride: number, relativeCoords: boolean)
