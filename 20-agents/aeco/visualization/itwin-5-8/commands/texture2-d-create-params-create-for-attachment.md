# texture2-d-create-params-create-for-attachment

Lifecycle: single

Texture2DCreateParams.createForAttachment(width: number, height: number, format: GL.Texture.Format, dataType: GL.Texture.DataType)
