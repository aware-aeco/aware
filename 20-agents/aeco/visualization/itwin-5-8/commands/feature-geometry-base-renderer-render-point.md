# feature-geometry-base-renderer-render-point

Lifecycle: single

FeatureGeometryBaseRenderer.renderPoint(geometryLengths: number[], geometryCoords: number[], stride: number, relativeCoords: boolean)
