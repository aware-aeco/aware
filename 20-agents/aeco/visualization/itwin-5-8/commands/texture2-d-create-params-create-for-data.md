# texture2-d-create-params-create-for-data

Lifecycle: single

Texture2DCreateParams.createForData(width: number, height: number, data: Texture2DData, preserveData?: boolean, wrapMode?: GL.Texture.WrapMode, format?: GL.Texture.Format)
