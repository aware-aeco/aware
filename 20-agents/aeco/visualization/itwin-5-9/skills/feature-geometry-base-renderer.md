---
name: core-frontend-feature-geometry-base-renderer
description: FeatureGeometryBaseRenderer declarations from core-frontend
---

# FeatureGeometryBaseRenderer

## Methods

- `hasSymbologyRenderer()`
- `beginPath()`
- `closePath()`
- `lineTo(x: number, y: number)`
- `moveTo(x: number, y: number)`
- `stroke()`
- `fill()`
- `drawPoint(x: number, y: number)`
- `finishPoints()`
- `renderPath(geometryLengths: number[], geometryCoords: number[], fill: boolean, stride: number, relativeCoords: boolean)`
- `renderPoint(geometryLengths: number[], geometryCoords: number[], stride: number, relativeCoords: boolean)`
- `hasSymbologyRenderer()`
- `beginPath()`
- `closePath()`
- `lineTo(x: number, y: number)`
- `moveTo(x: number, y: number)`
- `stroke()`
- `fill()`
- `drawPoint(x: number, y: number)`
- `finishPoints()`
- `renderPath(geometryLengths: number[], geometryCoords: number[], fill: boolean, stride: number, relativeCoords: boolean)`
- `renderPoint(geometryLengths: number[], geometryCoords: number[], stride: number, relativeCoords: boolean)`
