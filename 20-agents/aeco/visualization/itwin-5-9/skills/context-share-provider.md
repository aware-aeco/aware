---
name: core-frontend-context-share-provider
description: ContextShareProvider declarations from core-frontend
---

# ContextShareProvider

## Methods

- `isProviderUrl(tilesetUrl: string)`
- `getInfoFromUrl(tilesetUrl: string)`
- `getInfoFromBlobUrl(blobUrl: string)`
- `isProviderUrl(tilesetUrl: string)`
- `getInfoFromUrl(tilesetUrl: string)`
- `getInfoFromBlobUrl(blobUrl: string)`
