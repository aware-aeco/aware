---
name: core-frontend-vertex-indices
description: VertexIndices declarations from core-frontend
---

# VertexIndices

## Methods

- `fromArray(indices: number[])`
- `encodeIndex(index: number, bytes: Uint8Array, byteIndex: number)`
- `setNthIndex(n: number, value: number)`
- `decodeIndex(index: number)`
- `decodeIndices()`
- `fromArray(indices: number[])`
- `encodeIndex(index: number, bytes: Uint8Array, byteIndex: number)`
- `setNthIndex(n: number, value: number)`
- `decodeIndex(index: number)`
- `decodeIndices()`
