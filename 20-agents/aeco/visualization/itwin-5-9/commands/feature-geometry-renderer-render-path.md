# feature-geometry-renderer-render-path

Lifecycle: single

FeatureGeometryRenderer.renderPath(geometryLengths: number[], geometryCoords: number[], fill: boolean, stride: number, relativeCoords: boolean)
