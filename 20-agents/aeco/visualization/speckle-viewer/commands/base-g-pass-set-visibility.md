# base-g-pass-set-visibility

Lifecycle: single

BaseGPass.setVisibility(objectVisibility: ObjectVisibility | null, visibilityFunction?: (renderer: SpeckleRenderer) => Record<string, BatchUpdateRange>)
