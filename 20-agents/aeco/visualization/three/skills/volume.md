---
name: three-volume
description: Volume declarations from three
---

# Volume

## Methods

- `getData(i: number, j: number, k: number)`
- `access(i: number, j: number, k: number)`
- `reverseAccess(index: number)`
- `map(functionToMap: () => void, context: this)`
- `extractPerpendicularPlane(axis: string, RASIndex: number)`
- `extractSlice(axis: string, index: number)`
- `repaintAllSlices()`
- `computeMinMax()`
