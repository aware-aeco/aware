---
name: three-node-material
description: NodeMaterial declarations from three
---

# NodeMaterial

## Methods

- `setValues(values?: NodeMaterialParameters)`
- `build(builder: NodeBuilder)`
- `setupObserver(builder: NodeBuilder)`
- `setup(builder: NodeBuilder)`
- `setupClipping(builder: NodeBuilder)`
- `setupHardwareClipping(builder: NodeBuilder)`
- `setupDepth(builder: NodeBuilder)`
- `setupPositionView(builder: NodeBuilder)`
- `setupModelViewProjection()`
- `setupVertex(builder: NodeBuilder)`
- `setupPosition(builder: NodeBuilder)`
- `setupDiffuseColor(builder: NodeBuilder)`
- `setupVariants(builder: NodeBuilder)`
- `setupOutgoingLight()`
- `setupNormal()`
- `setupEnvironment(builder: NodeBuilder)`
- `setupLightMap(builder: NodeBuilder)`
- `setupLights(builder: NodeBuilder)`
- `setupLightingModel()`
- `setupLighting(builder: NodeBuilder)`
- `setupFog(builder: NodeBuilder, outputNode: Node)`
- `setupPremultipliedAlpha(builder: NodeBuilder, outputNode: Node)`
- `setupOutput(builder: NodeBuilder, outputNode: Node)`
- `setDefaultValues(material: Material)`
- `copy(source: NodeMaterial)`
