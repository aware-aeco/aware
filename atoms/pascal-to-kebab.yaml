id: pascal-to-kebab
kind: map
description: Convert `PascalCase` → `kebab-case`.
inputs:
  - name: text
    type: string
output:
  type: string
code: |
  (text) => String(text || '')
    .replace(/([a-z0-9])([A-Z])/g, '$1-$2')
    .replace(/([A-Z]+)([A-Z][a-z])/g, '$1-$2')
    .toLowerCase()
